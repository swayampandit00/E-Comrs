
  # E-commerce website

  This is a code bundle for E-commerce website. The original project is available at 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
